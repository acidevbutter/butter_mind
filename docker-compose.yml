services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_DB: butter_mind
      POSTGRES_USER: butter_mind
      POSTGRES_PASSWORD: butter_mind
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U butter_mind"]
      interval: 5s
      timeout: 5s
      retries: 5

  api:
    build: .
    env_file: .env
    environment:
      DATABASE_URL: postgresql+asyncpg://butter_mind:butter_mind@db:5432/butter_mind
    ports:
      - "8001:8000"
    depends_on:
      db:
        condition: service_healthy
    networks:
      default:
      devbutter_shared:
        aliases:
          - mind-api

volumes:
  pgdata:

networks:
  default:
  devbutter_shared:
    external: true
